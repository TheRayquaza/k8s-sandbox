kind: Service
apiVersion: v1
metadata:
  name: pinchflat-service
  namespace: pinchflat
  labels:
    app: pinchflat
spec:
  type: ClusterIP
  selector:
    app: pinchflat
  ports:
    - protocol: TCP
      port: 8945
      targetPort: 8945
